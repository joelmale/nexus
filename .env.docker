# Docker/K8s Environment Configuration
# Copy this to .env when running in containers

# WebSocket Server Configuration
# In Docker, set to the service name (e.g., 'backend' or 'ws-server')
# In K8s, set to the service DNS name
VITE_WS_HOST=backend
VITE_WS_PORT=5000

# Asset Server URL
# In Docker Swarm/K8s, use the service URL
VITE_ASSET_SERVER_URL=http://backend:5000

# Development server ports (not used in production)
PORT=3000
WS_PORT=5000
